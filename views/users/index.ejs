<%-include('../base/header')%>

<body>

    <div>
        Please log in 
    </div>

    <!-- <% if (typeof user !== 'undefined') { %>
    <span style="margin-right: auto;">Welcome, <%= user.name %></span>
    <a href="/logout">Log Out</a>
    <% } else { %>
    <a href="/auth/google">Login with Google</a>
    <% } %> -->
  </body>
</html>
